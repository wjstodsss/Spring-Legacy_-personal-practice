<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.productMapper">
    <select id="getProductList" resultType="product">
        SELECT * FROM tbl_product
    </select>
    
    <delete id="delete" parameterType="int">
        delete from tbl_product where id=#{id}
    </delete>
    
     <select id="getOne" parameterType="int" resultType="product">
        SELECT * FROM tbl_product where id=#{id}
    </select>
    
    <update id="update" parameterType="product">
	    UPDATE tbl_product
	    SET name = #{name}, description = #{description}, price = #{price}, category = #{category}, imageUrl = #{imageUrl}
	    WHERE id = #{id}
	</update>
</mapper>